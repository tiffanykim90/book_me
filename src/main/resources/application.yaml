spring:
  datasource:
    username: book_me
    password: your_password
    url: jdbc:mysql://localhost:3306/book_me
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always