spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_me
    username: book_me
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  sql:
    init:
      mode: never