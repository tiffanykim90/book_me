spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_me
    username: book_me
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: never